using System;

namespace Samarium.MailProcessor {

    using PluginFramework;
    using PluginFramework.Command;
    using PluginFramework.Plugin;

    using System.Collections.Generic;
    using System.Linq;
    using System.Text;
    using System.Threading.Tasks;

    partial class MailProcessor {

        ICommandResult Command_MpConfig(IPlugin sender, ICommand command, params string[] args) {
            return default;
        }

        ICommandResult Command_MpStart(IPlugin sender, ICommand command, params string[] args) {
            return default;
        }

        ICommandResult Command_MpStop(IPlugin sender, ICommand command, params string[] args) {
            return default;
        }

    }
}
